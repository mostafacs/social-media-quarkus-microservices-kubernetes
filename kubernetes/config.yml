apiVersion: v1
kind: ConfigMap
metadata:
  name: social-configs
  namespace: default
data:
  db-host: UyFCXCpkJHpEc2I9
  username: YWRtaW4=

---

apiVersion: v1
kind: Secret
metadata:
  name: social-secrets
  namespace: default
data:
  db-password: YWRtaW4=

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app.kubernetes.io/name: social-ingress
    app.kubernetes.io/version: "1.0"
  name: social-ingress
spec:
  rules:
    - host: social-test.app
      http:
        paths:
          - path: /user
            pathType: Prefix
            backend:
              service:
                name: user
                port:
                  number: 8010
          - path: /feed
            pathType: Prefix
            backend:
              service:
                name: feed
                port:
                  number: 8020